# Reorganisation, 2026-08-07 — old path → new path map

Done in one pass with git tracking every move (`git log --follow <file>` shows
any file's full history across the move). The rule applied: **no cited path was
broken above the folder level.** Slice JSONs and governing docs cite paths like
`EU/slices/_staging/...` and `EU/<name>_PartA_*.md` as provenance; the first
kind is untouched, the second kind moved exactly one level down inside `EU/`
with filenames unchanged, so any stale citation resolves by prefixing the
subfolder named below.

## The map

| Old | New |
|---|---|
| `BACKLOG.md` | `planning/BACKLOG.md` |
| `EXPANSION-V1.md` | `planning/EXPANSION-V1.md` |
| `TODO LISTS/rolling-todo.md` | `planning/rolling-todo.md` (`TODO LISTS/` removed) |
| `eu_ids_block.txt` | `archive/scratch/eu_ids_block.txt` |
| `research-input/grok-research-handoff-2026-07-31.zip` | `archive/scratch/` (the extracted folder stays in `research-input/`) |
| `EU/SEC00.pdf` … `EU/SEC10.pdf` | `EU/sources/` |
| `EU/EU Meta jsons.docx`, `EU/Soft Connections.docx`, `EU/Research.eu.docx`, `EU/Research.2.md.docx` | `EU/sources/` |
| `EU/G.00.md.docx`, `EU/G.02.docx` … `EU/G.09.docx`, `EU/G_11.docx`, `EU/G_13.docx` | `EU/legacy-handoffs/` — **these are the only copies of EU hand-offs 00–13**; not junk, just not .md |
| `EU/*_PartA_*.md`, `EU/PartB_soft_connections_2026-08-04.*`, `EU/AnnexB_assessment_2026-08-05.md` | `EU/extraction/` |
| `EU/Research.1-superseded_2026-08-05.md`, `EU/Research.2-merged-DRAFT_2026-08-05.md` | `EU/extraction/` |
| `EU/GROK-PROMPT*.md`, `EU/GROK-BRIEF*.md` | `EU/prompts/` |

## Deliberately NOT moved

- `EU/G.*.md` + `EU/G.*.json`, `NZ/`, `AU/`, `sessions/` — every hand-off
  cross-reference (`NZ/G.4.md`, `AU/G.1.md`, "highest-numbered `V0.*` in
  `sessions/`") still resolves. The hand-off spec block ("How to write the next
  hand-off") needs **no** path changes.
- `EU/slices/` including `_staging/` — cited verbatim as provenance in slice
  JSONs (`src/data/research/*.json`) and in `Research.1.md`. Do not move it.
- `src/`, `scripts/`, all app config at root — the app's imports and Vite's
  expectations are position-dependent.
- `README.md`, `START-HERE.md`, `REPORTS.md`, `Research.1.md` — the root entry
  points.

## Changes made alongside the moves (same session)

- `scripts/handoff-to-json.py` — batch mode and the hook previously scanned
  **only** `EU/`; NZ and AU sidecars could silently drift stale. Now scans all
  of `BRANCHES = ["EU", "NZ", "AU"]`. Verified: `--check` reports every sidecar
  in all three branches up to date.
- `scripts/validate-data.ts` — ✗ invariant failures now fail the exit code
  (previously printed but exited 0 — the "permanently-red validator" problem
  `NZ/G.4.md` flagged). Also now prints duplicate report ids (previously
  collected but never shown anywhere — a silent-data-loss channel).
- `src/App.tsx` — duplicate-edge console message said "first kept"; the rule is
  last-wins (V0.8). Fixed, and duplicate report ids now warned too.
- `src/data/research/grok-r8-accession-belt.json` — the Montenegro `_dropped`
  note (`me-monstat-national-accounts -> esa-2010`) converted to a caveat-note
  with null endpoints, closing `NZ/G.4.md` cheap check 3. The regulation-number
  discrepancy it records is still unresolved and still needs a primary-source
  check; the note says so.
- `.gitignore` — added `_to_delete/` (a workaround folder: the remote session's
  file bridge cannot delete files, so removals get moved there for Thomas to
  empty by hand).

After all of the above: `npm run check` clean, `npm run validate` prints zero ✗
and exits 0, `python3 scripts/handoff-to-json.py --check` reports no drift.

## For the next research session (any branch)

Nothing in your spec block changes. One thing to know: `npm run validate` now
*fails* (exit 1) on any ✗, including a `_dropped` note that contradicts the
graph — the workaround for an intentional caveat-note is unchanged (null
endpoints), and cheap check 5 in `NZ/G.4.md` (a proper `reason` value for
caveat-notes) is still the real fix.
