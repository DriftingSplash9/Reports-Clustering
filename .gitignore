node_modules/
dist/
*.local
.DS_Store
_to_delete/

# Generated 2026-08-21 by scripts/gen-slices.ts, from src/data/research/ —
# same status slices.generated.ts had before it (regenerated by the
# predev/prebuild/validate hooks every time, so it doesn't need to be
# committed for the app to build). ~8MB and pure derivative; not tracking it
# keeps the repo from carrying the research corpus twice.
public/corpus-data.json

# Word temp lock files
~$*
