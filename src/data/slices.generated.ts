// RETIRED 2026-08-21 (§6 item 4 of the full review) — this file is no longer
// written or read by anything.
//
// It used to be AUTO-GENERATED by `scripts/gen-slices.ts`: one `import`
// statement per file in `src/data/research/`, which is what compiled the
// whole 8.2MB research corpus into the browser's JS bundle as executable
// module code. `gen-slices.ts` now emits `public/corpus-data.json` instead
// (see that script's own comment for the full reasoning), and the corpus is
// read by `src/data/index.ts` (Node, off disk) and `src/data/browserCorpus.ts`
// (browser, via `fetch`) — neither imports this file any more.
//
// Left in place, stale, rather than deleted: this session has no shell
// access to Thomas's machine, only file read/write through the device
// bridge, so it cannot `mv` this into `_to_delete/` itself (standing rule —
// see HANDOFF.md §2 rule 6). Move it there next time an agent with shell
// access touches this project. Safe to delete outright; nothing imports it.
export {}
