# Prompt — closing blocked-domain leads from the 2026-08-29 JP/KR/SA/TR unlinked-node round

Attach `GROKREADME.md` (from `notes/grok-research-queue-2026-08-22/`) alongside this file — same standing rules as always. This is a small, targeted follow-up, not a fresh country pass: our own agents already found strong indexed or partial evidence for each of the leads below, but couldn't read the actual primary-source page (403/Cloudflare block, a JS-only page shell, or a signed/expiring download URL our fetch tool couldn't follow). You may have better luck with a different access route, a cached/alternate copy, or a different page on the same site that says the same thing. If you can't get in either, an honest "still can't reach it" is a fine answer — don't manufacture a quote.

## Real corpus ids you may use (source or target) — copy exactly, do not invent variants

`jp-nra`, `jp-nra-reactor-status`, `jp-fukushima-daiichi-decommissioning`, `jp-spent-fuel-storage`, `jp-reconstruction-agency`, `jp-estat`, `jp-statistics-act-2007`, `jp-long-term-care-insurance`, `jp-comprehensive-survey-living-conditions`, `kr-financial-stability`, `bis-basel-framework`, `sa-pif`, `sa-national-accounts`, `tr-industrial-production`, `isic`, `tr-health`, `who-icd-10`

## The leads

1. **Japan — the nuclear-regulatory cluster (`jp-nra`, `jp-nra-reactor-status`, `jp-fukushima-daiichi-decommissioning`, `jp-spent-fuel-storage`)** (expected `methodology_depends_on` or `cites`, direction TBD by whichever document you find). `jp-nra-reactor-status`'s own page (nra.go.jp/english/nuclearfacilities/operation.html) is JS-rendered/table-driven and returned nothing to our fetch tool. `jp-nra` itself (the Nuclear Regulation Authority) wasn't independently researched at all this round — genuinely open ground, not a confirmed dead end. `jp-fukushima-daiichi-decommissioning` and `jp-spent-fuel-storage` very plausibly cite NRA oversight/reporting requirements but no document was opened to check. Find any real, fetchable NRA or TEPCO document that names a specific reporting/oversight relationship among these four, and quote it verbatim with a URL.

2. **Japan — `jp-estat` → `jp-statistics-act-2007`** (expected `cites`). e-Stat is Japan's official statistics portal; its English homepage, Terms of Use page, and an ESRI guide page were all checked and none state its Statistics Act legal basis in fetchable text (one candidate URL 404'd). Find the portal's actual "About e-Stat" or legal-basis page and quote the Statistics Act reference verbatim, with a URL.

3. **Japan — `jp-long-term-care-insurance` / `jp-comprehensive-survey-living-conditions`** (expected `uses_data_from` or `calculated_from`). MHLW's Comprehensive Survey of Living Conditions (国民生活基礎調査) is the likely source for the Long-Term Care Insurance Business Status Report figures, but the MHLW Handbook of Health and Welfare Statistics index page we could fetch was a bare table of contents with no citation text, and the underlying charts are image-only (GIF) with no source note in the accompanying CSV. Find the actual 介護保険事業状況報告 methodology note or MHLW cross-reference stating this, and quote it verbatim with a URL.

4. **South Korea — `kr-financial-stability` → `bis-basel-framework`** (expected `methodology_depends_on`). We found the exact right document — a Bank of Korea Financial Stability Report appendix titled "주요국의 바젤Ⅲ 최종안 도입 현황 및 시사점" (on Basel III implementation) — but bok.or.kr's file-download endpoint redirects to a signed, short-lived `file-cdn.bok.or.kr` URL our fetch tool couldn't follow (empty response after the redirect). Find a working copy of this same document (or another BOK page stating the same Basel-framework dependency) and quote the relevant sentence verbatim, with a URL.

5. **Saudi Arabia — `sa-pif` → `sa-national-accounts`** (expected `uses_data_from`). PIF's own site (pif.gov.sa) 403s our fetch tool on its Annual Report 2024 and Consolidated Financial Statements 2024 PDFs (Cloudflare-style bot block). These documents very plausibly state GDP/national-accounts figures sourced from GASTAT. Find a working copy (search-engine cache, a filing mirror, a press release quoting the same figures with attribution) and quote the GASTAT/national-accounts attribution verbatim, with a URL.

6. **Türkiye — `tr-industrial-production` → `isic`** (expected `methodology_depends_on`). Türkiye's Industrial Production Index is classified per NACE Rev.2, itself derived from ISIC Rev.4 — a plausible chain — but every TurkStat classification/methodology page we tried was unreachable (SPA-gated or dead). Find a working TUIK methodology document or metadata page stating the NACE Rev.2 ↔ ISIC Rev.4 basis for this specific index, and quote it verbatim with a URL.

7. **Türkiye — `tr-health` → `who-icd-10`** (expected `methodology_depends_on`). TurkStat's Death and Cause of Death Statistics bulletin (which would plausibly cite ICD-10 for cause-of-death coding) is only reachable via veriportali.tuik.gov.tr, which serves a "JavaScript required" shell to both our fetch tools. Find a working copy of the bulletin's methodology section or metadata page and quote the ICD-10 statement verbatim, with a URL.

## Reply

Same as always — one JSON file per GROKREADME.md's schema (`_meta`/`proposed_reports`/`dependencies`), rendered as a download, not pasted into chat. No new nodes should be needed for any of these — everything targets an id already listed above (the Japan nuclear cluster is the one exception where a new node might genuinely be needed if you find a document naming a report not yet in our corpus — propose it fully rather than inventing an id). If a lead genuinely dead-ends, say so in `_meta.countries_thin_or_null` (or an equivalent note) rather than stretching for a quote.
