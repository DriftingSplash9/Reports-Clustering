# Prompt for the next agent(s) — Reports Clustering, after the Basel III round (2026-09-04)

Paste the block for the round you want run. Each is self-contained; the
"Standing" header is the same for all of them.

---

## Standing (paste at the top of every round)

You are working in the Reports Clustering repo (Economic Report Influence
Graph). Before touching anything, read in this order: `HANDOFF.md` →
`PLAYBOOK.md` §2 (rules), §6 (traps), §7 (standing decisions) →
`notes/Midvamp - Revamp.md` → `notes/basel-iii-round-2026-09-04.md` →
`notes/rulings-round5-2026-09-04.md` → project memory
`basel_iii_round_2026-09-04` and `rulings_round5_2026-09-04`.

Rules that get broken: **never run git, not even `git status`**, and never
state git status in a document; never delete a file (`mv` to `_to_delete/` and
log it); never hand-edit `public/corpus-data.json` (it is generated by
`npm run gen`); `npm run validate` before and after any data change; a pointer
is not a source — raw-verify the HTTP status before trusting a quote; no
document saying so, no edge.

How to run the toolchain: copy the repo (minus `node_modules/`, `.git/`,
`archive/`, `evidence-cache/`, `tmp_work/`, `_to_delete/`, `Claude outputs/`)
to `$HOME/rc-scratch` on the bridge VM — **outside `mnt/`** — `npm install`
there, then symlink `src/data/research/` and `evidence-cache/` from the scratch
copy back into the mounted repo so `--write` lands in the corpus. **Copy
`scripts/`, never symlink it** (it resolves ROOT through `realpath` and would
put `.evidence-fulltext/` inside the mounted repo, where rule 6 then forbids
deleting it). Budget ~4 minutes for the install. Nothing can be backgrounded;
runs over ~170 s are re-run as repeated foreground calls (the grader resumes
from its fulltext cache). The grader is `npx tsx scripts/grade-evidence.ts`;
`--edges <json[#key]>` selects `{source,target}` rows, `--json <out>` writes
results, **`--selftest` must stay 47/47**. The bridge VM has working outbound
network (verified against bis.org, 2026-09-04) and it is a **different network
from the cloud sandbox — neither is a superset**. Re-probe every host; routing
has decayed between machines or within a day three times running.

Grading discipline: a re-grade writes improvements only; regressions go to a
dated JSON in `Claude outputs/` with host and reason. **Never `--write` a grade
that fell because of a fetch failure on your machine** — that is a machine
fact, not a corpus fact, and the recorded grade came from a successful read. A
`quote-not-in-document` on a document read in full (HTTP 200) means the quote
is wrong, not the network — revert the quote, leave the grade. A document read
via an archived snapshot, OCR or a signed-token PDF caps at B. The direction of
an edge is never checked by the grader — flag direction conflicts for Thomas,
don't flip them. **Never edit a `basis` to move a grade.** If an evidence
record is graded down by a matcher defect, fix or report the matcher; trimming
the record is grade-motivated editing and it hides the defect from everyone
after you.

When done: verify (`npm run validate` exit 0, `tsc --noEmit` clean, 123/123
logic, selftest 47/47, `npm run gen`), write the round's narrative to
`notes/<round>-2026-09-0X.md` **and** project memory, add any new trap/rule to
PLAYBOOK §6/§7, then hand off per HANDOFF §4: archive the old HANDOFF first
(sha256-verified copy under `archive/Previous Handoffs/`), then rewrite
`HANDOFF.md` in place, state only, under ~10k characters, §4 kept verbatim.

Thomas's working style: direct, no padding, no praise. Tell him plainly when
something failed or when a prediction you made turned out wrong — that is more
useful to him than a tidy summary. Don't ask him to commit; he knows.

---

## Round 1 — the checksummed transport (do this first)

HANDOFF §3 agent item 1, and the binding constraint on 20 queued edges.

Three document sets read fine in Chrome and cannot be written to disk:
`www.anstat.ci` (2 edges — the document confirms BOTH claims, "NOUVELLE
NOMENCLATURE COICOP 2018" and the EHCVM pondérations sentence), `mid.ru` (1),
and the five BPS PDFs (17 edges, listed in
`Claude outputs/browser-pass-bps-psa-2026-09-04.json` → `refused`).

Round 3 recorded that "a `get_page_text` result over ~50 KB is persisted to a
file the sandbox can read". **It fires unpredictably** — round 4 padded past
50 KB and it did not force the persist. **Do not hunt for a bigger pipe.**
Have the page compute a SHA-256 of the extracted text, move the text by any
channel including a heredoc, and verify the checksum after writing. §6's
"retyping is lossy" warning stops applying the moment a copy checksums.

Then run the 17 BPS edges: ruled 2026-09-04 (option 1) — cite the landing page,
quote the PDF, record `via: token-pdf <date>`, grade caps at B. The route is
already in `routeCapsGrade()`.

---

## Round 2 — the two grader rulings from the Basel round

**Both are measured and neither is applied. Both need Thomas's sign-off AND a
corpus-wide before/after, which means a populated `.evidence-fulltext/` — so do
this in a session that already has one, not a fresh VM.** Full detail and the
measurements are in `notes/basel-iii-round-2026-09-04.md`.

1. **The A bar should test the window around *any* matched span, not only
   `bestSpan`.** `frb-regulation-q -> basel-iii` has quote coverage 1.00 and
   naming true and still grades B, because its basis quotes the press release's
   own headline (index 26, page chrome) as well as the substantive sentence
   (index 8470, with "Basel III" 49 characters away). Both spans score 1.00 and
   the tie breaks to the first. Suspect a whole class here: many bases in this
   corpus open by naming the source document.
2. **`namesTarget`'s single-token path should read `title_aliases`, and its
   character class should include Hangul.** It currently iterates only
   `target.title`, and `/[぀-ヿ㐀-鿿]/` excludes Hangul entirely, so `바젤Ⅲ` —
   one token, because NFKD folds U+2162 to `III` giving `바젤iii`, and Hangul
   and Latin are both `\p{L}` so `tokenise` never splits it — is invisible to
   both that path and the ≥2-word run rule. The alias minted this round is
   attested but inert until this changes.

Measure both the way round 5 measured the whitespace pass: run over every
span-carrying edge with a readable document, report grades up / grades down,
and run a deliberately-mismatched (quote, document) control for false
positives. Report the numbers before adopting anything.

---

## Round 3 — the last 15 browser-pass edges

Worklist and per-host routing: `notes/browser-pass-round3-2026-09-04.md`,
`notes/browser-pass-round4-2026-09-04.md`, and
`Claude outputs/browser-pass-round3-worklist-2026-09-04.json`. **Re-probe the
routing table before touching a host.** Newest instance of decay:
`bok.or.kr` redirects its signed download to `file-cdn.bok.or.kr`, which the
bridge VM could not resolve on 2026-09-04, though round 4 recorded the host as
reachable from the cloud sandbox.

Also in this family, and research rather than plumbing: **the 6 India + 1
Tanzania re-cites**. They cite a department landing page, not the survey
(`pc.odisha.gov.in`, `des.assam.gov.in`, `descg.gov.in`,
`himachalservices.nic.in`, `indianrailways.gov.in`, `mod.gov.in`, `dcc.go.tz`).
An index page will never name the series — find the real document.

And the OCR job: `ru-minfin-prikaz-128n-gfs-procedure -> imf-sdds`, a scanned
PDF with 15 characters of text. Blocked on `rus.traineddata` (tesseract on both
machines carries only `eng`+`osd`). **Try fetching it on the bridge VM** — its
network works. Caps at B (`via: ocr tesseract <date>`).

---

## Round 4 — Basel II, if you want a cheap node

**One fetch decides it.** SBV's Financial Soundness Indicators table
(`sbv.gov.vn/en/web/sbv_portal/w/sbv606221`) cites Circulars 41/2016 and
22/2019 with explicit "Applying Basel II" language. The old `_dropped` entry
was aimed at `vn-npl` — Vietnam's domestic 5-group NPL classification, which
genuinely has no Basel basis — so it was dropped on the **wrong target**, not
on the naming. Aim it at a capital-adequacy node instead.

Worth one more fetch while you are there: `tw-financial-stability`, the obvious
Basel III analogue of the Korean edge — a national FSR that very likely names
the standard.

**No node without an edge.** Basel I has zero corpus documents naming it and
would be isolated on arrival; do not mint it. If you do mint `basel-ii`, note
that `basel-iii` is deliberately **standalone — no `part_of`** (Thomas,
2026-09-04); match that, and read PLAYBOOK §6 on titles as matcher input before
choosing a title.

---

## Smaller items, any session

- **F-14**: gate the weighted-vs-raw disagreement list to an authority floor
  (e.g. 0.05) so it reads as a check again rather than 40 lines of noise at the
  end of every validate. (F-13 is closed.)
- Doc hygiene: README:130, REPORTS:9–32, PLAYBOOK:18–20, START-HERE:31/37 —
  START-HERE's ranking sentence is audit F-07 and is now wrong in a NEW way,
  since `brics-jsp` left the top ten. Retire `check-urls.ts` into the grader.
- **Do not build the Round B CJK n-gram matcher.** Cancelled 2026-09-04 with
  measurements — the whitespace-insensitive pass in `locateQuote` already is
  the CJK fix (19 of its 34 grade moves were CJK).
